---
import { Image } from 'astro:assets';

const { title, url, description, pubDate, author, image, tags , index} = Astro.props;
---

<li>
  <a href={url} title={title} class="group">
    <article class="flex-1 h-full flex flex-col pr-3">
      <div class="block w-full lg:col-span-2">
        <Image
            class="aspect-[2/3] object-cover bg-center h-full w-full rounded-tr-xl rounded-tl-xl"
          src={image}
          alt={title}
          loading={index < 3 ? "eager" : "lazy"}
          width="480"
          height="620"
        />
      </div>
      <div
        class="flex flex-col items-start justify-between h-full flex-1 w-full p-4 bg-gray-100 rounded-bl-xl rounded-br-xl">
        <div>
          <div class="inline-flex space-x-1 items-center">
            <p class="text-xs font-medium text-black">{author}</p>
            <span aria-hidden="true">&middot;</span>
            <div class="flex text-xs text-zinc-400">
              <time datetime="2020-03-16">{pubDate}</time>
            </div>
          </div>
          <p class="text-black mt-2 font-display text-xl text-pretty">
            {title}
          </p>
        </div>
        <p class="mt-4 text-sm text-zinc-500 line-clamp-2">{description}</p>

        <div class="mt-4 flex space-x-2">
          {
            tags.map((tag: string) => (
              <span class="text-xs font-medium text-black">{tag}</span>
            ))
          }
        </div>
      </div>
    </article>
  </a>
</li>
